# Upload Manifest

Use these files for the anonymous external link, in this order. The protocol-mask evidence should be primary; the exact Gaussian diagnostic should be used only for validity/\(W\) questions; the synthetic forecasting diagnostic is backup and should not be foregrounded in the rebuttal.

1. `ATTACHMENT_GUIDE.md`
2. `figures/protocol_mask_schematic_clean.png`
3. `figures/protocol_efficiency.png`
4. `tables/protocol_masks_ci.md`
5. `figures/core_frontier_efficiency_regression.png`
6. `tables/core_frontier.md`
7. `figures/gaussian_exact_validity.png`
8. `tables/gaussian_exact_validity.md`

Backup/avoid foregrounding:

- `figures/rebuttal_hero_summary.png` includes both the synthetic diagnostic and protocol masks; use the protocol-specific figures above first.
- `figures/synthetic_profiles_allocations.png`, `figures/synthetic_diagnostics.png`, and `tables/synthetic_diagnostics_ci.md` are optional backup for the forecasting diagnostic, not the main empirical claim.
- `figures/core_frontier_efficiency_classification.png` is best kept as backup because the classification frontier invites more scrutiny than it helps.
- `figures/core_frontier_validity.png` reports mean mask failure from the completed broad run, not max-window failure.

PNG note: figure PNG exports already exist for the primary assets, including `figures/protocol_efficiency.png`. Keep Markdown tables as the primary table uploads unless the review system requires image-only attachments. If a table PNG is required, the first one to render should be `tables/protocol_masks_ci.md`.
