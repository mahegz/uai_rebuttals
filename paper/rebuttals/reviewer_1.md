We thank the reviewer for the constructive assessment. We agree that, for the all-ones mask with a fixed open-loop schedule, our constraint is an alpha-spending rule; we will state this directly. The contribution is the batch-conformal reduction around that primitive. Independence of batch-conformal p-values turns each pre-specified monitoring mask into an exact product constraint, or in log form `sum_t M_t s_t <= -log(1-alpha)`. A standard alpha-spending stream is the one-row special case. Nontrivial mask families impose simultaneous overlapping constraints, imply suffix/restart validity after monitored failures, and allow restricted closed-loop replanning from calibration data/features without past test labels.

Masks also need sharper motivation. They are not post-hoc tuning knobs; they encode an audit protocol fixed before deployment. Examples include rolling K-batch factory QA, shift-level radiology review, scheduled high-risk periods, sparse maintenance audits, and contractual service windows. In these settings mask validity answers: what is the probability of any error in the monitored window? Restart validity is needed because deployment continues after a failure; the future monitored stream should retain level-alpha control instead of becoming vacuous. We will add such examples before the definition of mask validity and clarify ex ante specification.

Efficiency sensitivity is real and expected. Different masks are different guarantees: larger K or richer mask families consume more log-budget and produce larger sets. Validity is unaffected once the family is fixed ex ante, but the user pays for the strength of the monitoring query. We will separate the audit-family choice from the optimizer that spends its budget efficiently.

Experiment/link slot, to fill only with validated runs: report a compact operating frontier over K or protocol-defined masks, with window failure and set size side by side. If UAI/OpenReview permits anonymous external material, full supplemental plots/tables can go at [anonymous figure link: TBD]; the rebuttal itself will remain self-contained and will not rely on unverified numbers.

Minor corrections: fix "Independenceconformal" and replace or qualify "admissible" as "feasible" to avoid conflict with statistical admissibility.
