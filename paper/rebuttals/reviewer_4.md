We thank the reviewer for highlighting the relation to alpha-spending and the operational meaning of masks.

We agree that the all-ones-mask case is closely related to classical alpha-spending, and we will say so explicitly. The contribution is not to rebrand alpha-spending, but to show that batch-conformal $p$-value independence turns a family of monitoring requirements into exact finite-sample constraints: for every $M\in\mathcal M$, $\sum_t M_t s_t\le -\log(1-\alpha)$. This recovers alpha-spending for the all-ones mask and marginal coverage for singleton masks, but also covers intermediate families such as rolling windows. Restart validity is also stronger than first-error control: it rules out procedures that spend all validity after one failure and implies geometric decay of repeated monitored errors. We will revise Sections 2--4 to present the method as structured alpha-spending over mask families, with restart validity and label-free restricted closed-loop deployment as added guarantees.

Masks are not intended to be tuned after seeing outcomes. They encode the monitoring question fixed before deployment. We will add concrete examples: regulatory audits of every contiguous $K$-batch window, where $K$ is mandated; hospital/factory shifts, where masks are calendar-defined before labels arrive; and incident or scheduled high-risk monitoring, where the system continues after a failure and needs a renewed guarantee. Choosing $\mathcal M$ is analogous to choosing a horizon or subgroup family. We will add a short guide explaining that larger or denser mask families consume more log-budget and therefore require larger sets.

This also reframes mask sensitivity. Efficiency should depend on the guarantee requested: a longer rolling window is stricter than a singleton step, so the schedule must be more conservative. We will revise the experimental discussion accordingly, while leaving quantitative additions to the unified experiment pass.

Experiment/link slot: reserve only a compact validated sensitivity summary in the rebuttal. If venue policy permits, extended frontiers and protocol-mask schematics can be hosted anonymously at [anonymous figure link: TBD].

We will fix the Proposition 2.7 title typo ("Independence of conformal $p$-values") and replace "admissible" with "feasible" or "deployable" to avoid the statistical meaning of admissibility.
