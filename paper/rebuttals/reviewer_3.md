We thank the reviewer for the careful reading. We agree the draft should distinguish our target from ordinary alpha-spending more sharply.

Our claim is not that alpha-spending is absent, but that usual alpha-spending answers a narrower query: it allocates levels to a fixed stream and controls a first crossing. We require simultaneous validity over an ex ante family of monitoring masks and restart validity: after a monitored error at time \(s\), future monitored errors still have \(\alpha\)-level control. This rules out first-failure guarantees that are silent after failure and yields the geometric repeated-error bound. The log-budget program appears only after using independence of batch-conformal \(p\)-values to turn every mask constraint into an exact product constraint; it also permits restricted closed-loop replanning from calibration data without past test labels. We will revise the intro and scheduling section to state that alpha-spending is a primitive, while the contribution is the mask/restart-valid target, exact product/log-budget characterization, and cost-aware replanning.

Masks are meant as ex ante audit protocols, not post hoc tunable hyperparameters. We will add examples: a factory or clinical service audited on every \(K\)-batch rolling window; named shifts or weeks for compliance reporting; scheduled high-risk periods fixed from deployment metadata; and sparse maintenance checks. In these cases \(K\) or the mask family is dictated by reporting cadence or service-level requirements. Mask validity then matches the stakeholder's query: probability of at least one miss in that period, renewed after a miss because the system continues.

We agree that efficiency should depend on masks. A denser family asks for a stronger simultaneous guarantee, so wider sets are the cost; safety is unchanged for every feasible schedule. We will state this and separate empirical sensitivity from validity.

Experiment/link slot, to fill only with completed evidence: insert a brief frontier or table over operational mask choices, leaving full plots/tables for [anonymous figure link: TBD] only if anonymous external links are permitted. No numerical claim will be added before validation.

We will fix the Proposition 2.7 typo ("Independenceconformal") and rename "admissible" to "feasible" or "deployable" to avoid conflict with statistical admissibility.
