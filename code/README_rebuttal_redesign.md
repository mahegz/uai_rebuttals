# Rebuttal Code Redesign

`code/AnyhowCP-uai-2026-submission/` was reduced to a flat research-code core.
The rewrite removes old launchers, plotting scripts, model wrappers, abstract CP
classes, and PyTorch-heavy experiment code. The retained surface is the one the
rebuttal needs: masks, conformal p-values, log-budget constraints, SciPy/HiGHS
solvers, and a toy smoke check.

No paper rebuttal files were edited.
